apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: energy-company-dev
  labels:
    app: customer-service
spec:
  selector:
    app: customer-service
  ports:
  - protocol: TCP
    port: 5007
    targetPort: 5007
  type: ClusterIP

